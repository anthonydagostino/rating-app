# PostgreSQL
POSTGRES_USER=ratingapp
POSTGRES_PASSWORD=changeme_dev
POSTGRES_DB=ratingapp_db
POSTGRES_PORT=5432

# JWT (minimum 32 characters for HMAC-SHA256)
JWT__Secret=replace_with_at_least_32_char_secret_key_here_please
JWT__Issuer=RatingApp
JWT__Audience=RatingApp.Client
JWT__ExpiryHours=24

# Full connection string (alternative to individual vars)
# ConnectionStrings__DefaultConnection=Host=localhost;Port=5432;Database=ratingapp_db;Username=ratingapp;Password=changeme_dev
